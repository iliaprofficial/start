version: '3'

services:
  reactapp:
    build: ./
    container_name: reactapp
    restart: always
    ports:
      - '3003:3000'
    command: 'npm run start'
    environment: 
      - HOST=0.0.0.0
    networks:
      - leapper-corporate_default

networks:
  leapper-corporate_default:
    external: true

